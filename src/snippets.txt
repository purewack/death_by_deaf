sine wave
    
    static const float freq_delta = 440.f/48000.f;
    static float phase;
    for(int i=0; i<nframes; i++){
        outl[i] = 0.5f*std::sin(2.0f*3.1415f * phase);
        outr[i] = outl[i];
        phase += freq_delta;
        if (phase > 1.0f) phase -= 1.0f;
    }